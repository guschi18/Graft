# src/cli-epilogue.ts

Generates a closing message for the `graft init` command, including next steps and a visual wordmark.

- indigo · function · L23-L23 — Applies a specific color formatting to a string for terminal output.
- muted · function · L24-L24 — Applies a muted color formatting to a string for terminal output.
- Step · interface · L26-L31 — Defines the structure for a step in the epilogue, including a label and command.
- InitEpilogueOptions · interface · L33-L40 — Specifies options for rendering the init epilogue, including graph status and node/edge counts.
- formatInitEpilogue · function · L44-L85 — Formats and returns the next-steps epilogue for the `graft init` command based on provided options.
