# src/cli.ts

This file implements the `graft` CLI tool for building and maintaining a context graph from code, ensuring it stays in sync with the repository.

- engineFrom · function · L27-L30 — Creates an instance of the Graft engine configured with the specified context directory.
- fmt · function · L49-L53 — Formats a record of counts into a human-readable string for console output.
