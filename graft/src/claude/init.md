# src/claude/init.ts

- InitResult · interface · L9-L15 — interface InitResult
- runInit · function · L17-L49 — function runInit(dir: string, opts: { build?: boolean; cliPath?: string } = {}): InitResult
