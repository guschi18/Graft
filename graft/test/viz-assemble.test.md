# test/viz-assemble.test.ts

This file contains tests that validate the assembly of a context graph from markdown frontmatter, ensuring proper relation normalization and error handling for malformed files.

- makeContextDir · function · L12-L60 — This function creates a temporary directory with markdown files to simulate context data for testing the graph assembly.
